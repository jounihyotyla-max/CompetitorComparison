{
  "name": "@cc/shared",
  "version": "2.0.0",
  "private": true,
  "description": "Zod schemas, ids and pure helpers shared by the Cloud Functions and the web app.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9.0"
  }
}
